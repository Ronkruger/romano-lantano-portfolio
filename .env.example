# Keep this file as a reference for environment variables.
# Never commit .env files containing real secrets to version control.

# Example: API endpoints (if you add a backend later)
# VITE_API_URL=https://api.example.com

# Example: Third-party service keys (DO NOT commit these to git)
# VITE_SOME_SERVICE_KEY=your_key_here
